# Backups created by Claude Code
*.bak

# Large binary docs (keep in local docs/ only)
docs/*.pdf

# Photos (large binaries, not needed in repo)
pictures/

# ROS 2 colcon build artifacts (generated inside container, not on host)
build/
install/
log/

# Python
__pycache__/
*.pyc
*.pyo

# Editor
.vscode/settings.json
*.swp
*~
